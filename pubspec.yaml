name: lumide
description: A delightful cross-platform CLI for installing, managing, and launching the Lumide IDE.
version: 1.1.1
homepage: https://lumide.dev
repository: https://github.com/SoFluffyOS/lumide_cli

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  nocterm: ^0.6.0
  archive: ^4.0.9

executables:
  lumide:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
